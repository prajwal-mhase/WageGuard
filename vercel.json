{
  "comment": "This root vercel.json is intentionally minimal. The frontend is deployed from the frontend/ subdirectory using its own vercel.json. The backend is deployed separately to Render or Railway. Do NOT add Python runtime config here."
}
